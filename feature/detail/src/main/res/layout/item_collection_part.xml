<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/container"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:background="?attr/selectableItemBackground"
  android:clickable="true"
  android:focusable="true"
  android:paddingHorizontal="@dimen/default_margin_parent_20"
  android:paddingVertical="@dimen/margin_between_item">

  <com.google.android.material.imageview.ShapeableImageView
    android:id="@+id/ivMoviePoster"
    android:layout_width="64dp"
    android:layout_height="96dp"
    android:scaleType="centerCrop"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    app:shapeAppearanceOverlay="@style/ShapeAppearance.App.ImageView.Rounded"
    tools:srcCompat="@tools:sample/avatars" />

  <TextView
    android:id="@+id/tvMovieTitle"
    style="@style/TextAppearance.App.Body"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:textStyle="bold"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toEndOf="@id/ivMoviePoster"
    app:layout_constraintTop_toTopOf="parent"
    tools:text="Avatar: Fire and Ash" />

  <LinearLayout
    android:id="@+id/movie_meta"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginTop="4dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="@id/tvMovieTitle"
    app:layout_constraintTop_toBottomOf="@+id/tvMovieTitle">

    <TextView
      android:id="@+id/tv_movie_year"
      style="@style/TextAppearance.App.Chip"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:textColor="@color/gray_400"
      tools:text="2025" />

    <View
      android:id="@+id/meta_divider"
      android:layout_width="4dp"
      android:layout_height="4dp"
      android:layout_marginHorizontal="8dp"
      android:alpha="0.5"
      android:background="@color/gray_400" />

    <TextView
      android:id="@+id/tv_movie_rating"
      style="@style/TextAppearance.App.Chip"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:textColor="@color/gray_400"
      tools:text="7.6" />
  </LinearLayout>

  <TextView
    android:id="@+id/tvMovieOverview"
    style="@style/TextAppearance.App.Chip"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_marginTop="4dp"
    android:ellipsize="end"
    android:maxLines="2"
    android:textColor="@color/gray_300"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="@+id/movie_meta"
    app:layout_constraintTop_toBottomOf="@+id/movie_meta"
    tools:text="In the wake of the devastating war against the RDA and the loss..." />

</androidx.constraintlayout.widget.ConstraintLayout>