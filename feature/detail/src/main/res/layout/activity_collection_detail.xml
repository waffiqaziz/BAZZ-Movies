<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/gray_900"
  tools:context=".ui.CollectionDetailActivity">

  <com.google.android.material.appbar.AppBarLayout
    android:id="@+id/appBar"
    style="@style/Widget.App.AppBarLayout">

    <com.google.android.material.appbar.CollapsingToolbarLayout
      android:id="@+id/collapsing_toolbar"
      style="@style/Widget.App.CollapsingToolbarLayout"
      android:layout_width="match_parent"
      android:layout_height="220dp"
      app:collapsedSubtitleTextAppearance="@style/TextAppearance.App.CollapsingToolbar.Subtitle.Collapsed"
      app:collapsedSubtitleTextColor="@color/gray_300"
      app:expandedSubtitleTextAppearance="@style/TextAppearance.App.CollapsingToolbar.Subtitle.Expanded"
      app:expandedSubtitleTextColor="@color/gray_300"
      app:layout_scrollFlags="scroll|exitUntilCollapsed|enterAlways"
      app:maxLines="2"
      tools:title="Avatar Collection">

      <ImageView
        android:id="@+id/iv_collection_backdrop"
        style="@style/Widget.App.CollapsingToolbarLayout.ImageView"
        android:adjustViewBounds="true"
        android:contentDescription="@string/picture"
        app:layout_collapseMode="parallax"
        tools:layout_height="350dp"
        tools:srcCompat="@tools:sample/backgrounds/scenic" />

      <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        style="@style/Widget.App.Toolbar"
        android:paddingBottom="18dp"
        app:layout_collapseMode="pin">

        <Button
          android:id="@+id/btn_back"
          style="@style/Widget.App.Button.Circle.Back"
          android:layout_gravity="start|bottom"
          android:translationY="12dp" />

      </com.google.android.material.appbar.MaterialToolbar>
    </com.google.android.material.appbar.CollapsingToolbarLayout>
  </com.google.android.material.appbar.AppBarLayout>

  <androidx.core.widget.NestedScrollView
    android:id="@+id/scroll_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="vertical">

      <io.github.glailton.expandabletextview.ExpandableTextView
        android:id="@+id/tv_collection_overview"
        style="@style/TextAppearance.App.Body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/default_margin_parent_20"
        android:layout_marginTop="@dimen/margin_between_item"
        android:text="\n\n\n\n\n\n\n\n\n\n"
        app:animDuration="700"
        app:collapsedLines="5"
        app:ellipsizeTextColor="@color/yellow"
        app:isExpanded="false"
        app:readLessText="@string/read_less"
        app:readMoreText="@string/read_more"
        app:textMode="line"
        tools:ignore="HardcodedText"
        tools:text="The Avatar Collection tells the story of the world of Pandora, inhabited by the indigenous humanoids, the Na'vi. The main protagonist, Jake Sully, is sent to Pandora as an avatar—a hybrid of human and Na'vi. During his mission, Jake discovers the beauty and complexity of the Na'vi world and forms a deep connection with the planet's inhabitants. " />

      <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_genre"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="@dimen/margin_between_item"
        android:clipToPadding="false"
        android:paddingHorizontal="@dimen/default_margin_parent_20"
        tools:layoutManager="com.google.android.flexbox.FlexboxLayoutManager"
        tools:listitem="@layout/chip_genre"
        tools:orientation="horizontal" />

      <TextView
        android:id="@+id/tv_movies"
        style="@style/TextAppearance.App.Header3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/default_margin_parent_20"
        android:layout_marginVertical="@dimen/margin_between_item"
        android:letterSpacing="0.08"
        android:text="@string/movies"
        android:textAllCaps="true"
        android:textStyle="bold" />

      <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_collection_parts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:nestedScrollingEnabled="false"
        android:paddingBottom="@dimen/bottom_nav_min_height"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_collection_part" />

    </LinearLayout>
  </androidx.core.widget.NestedScrollView>

  <include
    android:id="@+id/illustration_error"
    layout="@layout/illustration_error"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_marginTop="@dimen/margin_to_centered"
    android:visibility="gone" />

  <com.google.android.material.loadingindicator.LoadingIndicator
    android:id="@+id/loading_indicator"
    style="@style/Widget.App.ProgressBar"
    tools:visibility="visible" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>